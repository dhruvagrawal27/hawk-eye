# Production / integration build defaults (vite build). Mocks OFF — the console binds to the
# real BACKEND API and Keycloak. PLATFORM injects the real URLs at deploy time; these are the
# safe in-cluster defaults. Never default mocks to true here (prompt §3.5, §10.4).
VITE_USE_MOCKS=false
