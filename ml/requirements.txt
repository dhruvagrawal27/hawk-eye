# Hawk-Eye ML — pinned-ish deps (mirror of pyproject). Core is light; heavy libs are
# import-guarded (ml/_optional.py) so every module imports without them.
# Versions resolved on Python 3.13 (no 3.12 wheels for the blueprint's torch/PyG 2.5/2.6).
numpy>=1.26
pandas>=2.2
scikit-learn>=1.5
scipy>=1.11

# Heavy (install per extra, e.g. `pip install -e ml/[all]`):
#   pyod>=2.0                       # L2 detectors
#   lightgbm>=4.3 xgboost>=2.0 catboost>=1.2 shap   # L3 GBDT + TreeSHAP / L5 XGB-Graph
#   torch>=2.5 torch_geometric>=2.6 # L4 sequence / L5 graph (resolved to 2.12/2.8 on py3.13)
#   mlflow>=2.16 evidently          # registry / drift
#   fairlearn                       # fairness metrics
#   onnxruntime onnx skl2onnx onnxmltools   # ONNX packaging for BACKEND serving
#   openai jinja2 fastapi uvicorn httpx     # TEE-LLM narrative gateway
#   featuretools dice-ml            # DFS / counterfactuals
#   pytest pytest-cov ruff black mypy       # dev tooling
