# Redis HA (PLATFORM-25, Part 30.1): primary + replica + Sentinel quorum. SCAFFOLD.
requirepass hawkeye_dev_pw
masterauth hawkeye_dev_pw
appendonly yes
# replica (on the standby node):
# replicaof redis-primary 6379
# sentinel (3 sentinels for quorum):
# sentinel monitor hawkeye-redis redis-primary 6379 2
# sentinel down-after-milliseconds hawkeye-redis 5000
